/**
 * Pi runtime — public surface.
 *
 * S04 exposes the extension entrypoint plus the package identity
 * and the S04 command / tool names so the runtime and tests can
 * refer to them by import.
 */
export { default, PACKAGE_NAME, PACKAGE_PHASE, PACKAGE_VERSION, ROLLOVER_COMMAND_NAME, ROLLOVER_TOOL_NAME, } from "./extension.js";
//# sourceMappingURL=index.d.ts.map